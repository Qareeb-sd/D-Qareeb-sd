# ✅ قائمة ما قبل الإطلاق — قريب (Qareeb)

> هذا الملف هو **المرجع الوحيد** لكل ما يجب ضبطه قبل الإطلاق الكثيف.
> راجعه كاملاً قبل فتح التطبيق للجمهور. كل بند يوضّح: لماذا، وأين، وكيف.

---

## 1. 🗺️ استضافة خادم الملاحة (OSRM) — مهم للإنتاج

**الوضع الحالي:** التطبيق يستخدم خادم OSRM العام المجاني
`https://router.project-osrm.org` (ممتاز للاختبار وأوّل أسابيع الإطلاق).

**المشكلة عند الاستخدام الكثيف:** خادم عام بلا ضمان توفّر — قد يبطئ أو **يحظر**
عنوان التطبيق مع كثرة الطلبات (مثلاً 1000 رحلة/يوم).

**الحل (بلا أي تعديل على الكود):**
1. استضِف OSRM على خادم صغير (VPS ~5–10$/شهر) بخريطة السودان من OpenStreetMap.
   - حمّل خريطة السودan: `sudan-latest.osm.pbf` من Geofabrik.
   - شغّل OSRM بالأمر المعتاد (osrm-extract / osrm-partition / osrm-customize / osrm-routed) أو عبر Docker.
2. أضِف سطراً واحداً في ملف `.env` (المحلي وعلى Cloudflare Pages):
   ```
   VITE_OSRM_URL=https://osrm.your-domain.com
   ```
3. أعِد البناء/النشر — انتهى. الكود يقرأ `OSRM_BASE_URL` من هذا المتغيّر تلقائياً
   (انظر `src/lib/maps.ts`).

**بديل بمميزات إضافية (صوت + زحام حيّ):** Mapbox Navigation أو Google Directions
(مدفوع بالطلب) — يتطلّب تعديل كود أكبر. غير ضروري للإطلاق.

---

## 2. 🔑 تقييد مفتاح خرائط Google

**الوضع الحالي:** المفتاح `AIzaSy...` في `.env` **غير مقيّد** (يعمل من أي مكان).

**الخطر:** أي شخص يلتقط المفتاح يستطيع استخدامه على حسابك.

**الحل (من لوحة Google Cloud Console):**
- قيّد المفتاح على:
  - **HTTP referrers** لنطاق الأدمن: `https://d-qareeb-sd.pages.dev/*`
  - وحزمة تطبيق أندرويد (Application restrictions → Android apps) ببصمة SHA-1.
- فعّل **حدّ يومي للطلبات** (Quota) لمنع الفواتير المفاجئة.
- فعّل فقط الـAPIs المستخدمة: Maps JavaScript API + Geocoding API.

---

## 3. ⚡ ترقية Supabase Compute (عند الحاجة)

قبل التدشين الكثيف، فعّل باقة **Large/XL** لقاعدة البيانات لتحمّل التزامن العالي
(1000 رحلة متزامنة). راجع لوحة Supabase → Settings → Compute.

---

## 4. 🔔 تفعيل Web Push (اختياري — للأدمن على الويب)

إشعارات المتصفح للوحة الأدمن (إن رغبت). إشعارات التطبيقات (FCM) تعمل بالفعل.

---

## 5. 🔐 تذكيرات أمان دائمة (لا تُخالَف أبداً)

- **مفتاح `service_role`** لا يوضع في العميل أو المستودع إطلاقاً — أسرار Supabase فقط.
- أسرار WhatsApp/Meta و `FCM_SERVICE_ACCOUNT` تبقى في **Supabase Secrets** فقط.
- مفتاح `anon` آمن للعميل (عام بطبيعته).

---

_آخر تحديث: يُحدّث مع كل تغيير يخصّ الإطلاق._
