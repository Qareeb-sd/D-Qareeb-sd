-- ============================================================
--  خصم الاشتراك الشهري للترحيل — نسبة خصم إضافية تُطبّق على الإجمالي
--  الشهري فقط (تشجيعاً للعميل على اختيار الخطّة الشهرية). كسر 0..1.
--  شغّل هذا المقطع مرّة واحدة.
-- ============================================================
alter table public.settings
  add column if not exists commute_monthly_discount numeric(5, 4) not null default 0;
